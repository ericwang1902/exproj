<table class="table table-hover">
     <thead>
      <tr>
         <th>手机号</th>
         <th>类型</th>
         <th>操作</th>
      </tr>
   </thead>
   <tbody>
       {{#each users}}
            <tr>
                <td>{{mobile}}</td>
                <td>{{usertype}}</td>
                <td>详情</td>
            </tr>
       {{/each}}

   </tbody>
   

</table>
   <nav>
    <ul class="pagination">
        </li>
        {{#each pagesArray}}
        <li><a href="/userlist?p={{p}}">{{p}}</a></li>
        {{/each}}
    </ul>
</nav>